server:
  port: 8080

spring:
  main:
    allow-bean-definition-overriding: true
가  redis:
    host: localhost
    port: 6379
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

