spring:
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.setge.orderapi: debug
    org.springframework: info