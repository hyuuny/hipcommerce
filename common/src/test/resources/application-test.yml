spring:
  output:
    ansi:
      enabled: always
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  jwt:
    secret: govlepel@$&

logging:
  level:
    com.setge.orderapi: debug
    org.springframework: info